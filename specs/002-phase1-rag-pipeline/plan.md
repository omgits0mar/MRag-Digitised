# Implementation Plan: Phase 1 — RAG Foundation Pipeline

**Branch**: `002-phase1-rag-pipeline` | **Date**: 2026-04-07 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/002-phase1-rag-pipeline/spec.md`
**Constitution Version**: 1.0.0

## Summary

Build the complete Phase 1 foundation for the MRAG platform: a data processing pipeline that ingests, chunks, enriches, and exports the Natural Questions dataset; a multilingual embedding system with FAISS vector indexing; and a basic retrieval pipeline that accepts natural language queries and returns ranked, metadata-enriched results. This constitutes the end-to-end data backbone from raw dataset to searchable knowledge base.

## Technical Context

**Language/Version**: Python 3.10+ (established in Feature 000)
**Primary Dependencies**: Pandas 2.2.3, NumPy 2.2.6, sentence-transformers 4.1.0, faiss-cpu 1.11.0, Pydantic 2.11.3, structlog 25.2.0
**Storage**: FAISS index files (`.faiss`) + JSON metadata files (no database in Phase 1)
**Testing**: pytest 8.3.5, pytest-asyncio 0.26.0, pytest-cov 6.1.1, scikit-learn 1.6.1 (for evaluation metrics)
**Target Platform**: Local development machine (Linux/macOS), CPU-only (GPU optional)
**Project Type**: Library (Python package) — CLI pipeline runners with programmatic API
**Performance Goals**: <200ms retrieval latency for K=10, <30 min full dataset processing, <5s index load
**Constraints**: Multilingual (50+ languages), offline-capable after dataset download, ~300K records
**Scale/Scope**: ~300K question-answer pairs, ~500K-1M chunks after splitting, single-node FAISS

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Article | Requirement | Status | Notes |
|---------|-------------|--------|-------|
| I — Modular Architecture | Independent modules with explicit I/O | PASS | Three distinct modules: `data/`, `embeddings/`, `retrieval/` with contract interfaces |
| II — Data Integrity | Validated preprocessing pipeline | PASS | Schema validation at ingestion, boundary checks between stages, deterministic chunking |
| III — Multilingual-First | Multilingual embedding model, UTF-8 throughout | PASS | `paraphrase-multilingual-MiniLM-L12-v2`, Unicode-safe chunking, UTF-8 encoding |
| IV — Retrieval Quality | Accuracy over speed, re-ranking beyond cosine | PASS | `IndexFlatIP` (exact search), weighted re-ranking, configurable top-K/thresholds |
| V — LLM as Contract | N/A for Phase 1 | SKIP | No LLM integration in Phase 1 |
| VI — Testing | Unit + integration tests per module | PASS | Unit tests for each component, integration tests for each pipeline, evaluation split created |
| VII — API Standards | N/A for Phase 1 | SKIP | No API endpoints in Phase 1 |
| VIII — Performance | Data-driven optimization, caching | PARTIAL | Batch embedding for throughput; caching deferred to Phase 2 (Feature 006) |
| IX — Code Quality | Type hints, black/ruff, structlog, pinned deps | PASS | All established in Feature 000; all new code follows same standards |
| X — Doc Separation | Spec (what) vs plan (how) | PASS | Spec is technology-agnostic; all tech decisions in this plan |

**Gate result**: PASS (Article VIII partial is acceptable — Phase 1 establishes performance baselines; caching is explicitly Phase 2 scope)

## Project Structure

### Documentation (this feature)

```text
specs/002-phase1-rag-pipeline/
├── plan.md              # This file
├── research.md          # Phase 0 output — technology decisions
├── data-model.md        # Phase 1 output — entity definitions
├── quickstart.md        # Phase 1 output — setup and usage guide
├── contracts/           # Phase 1 output — module interface contracts
│   ├── data-pipeline.md
│   ├── embedding-indexing.md
│   └── retrieval.md
├── checklists/
│   └── requirements.md  # Spec quality validation
└── tasks.md             # Phase 2 output (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
src/mrag/
├── __init__.py              # (existing) Package metadata
├── config.py                # (existing) Pydantic BaseSettings — already has chunk/embedding/retrieval config
├── logging.py               # (existing) structlog configuration
├── exceptions.py            # (existing) Exception hierarchy — already has DataProcessingError, EmbeddingError, RetrievalError
├── data/
│   ├── __init__.py          # (existing, empty)
│   ├── models.py            # NEW: ProcessedDocument, TextChunk, DocumentMetadata, PipelineResult
│   ├── ingestion.py         # NEW: load_dataset() — CSV/JSON loading with Pandas, schema validation
│   ├── chunking.py          # NEW: TextChunker — sliding window, sentence boundaries, configurable
│   ├── enrichment.py        # NEW: enrich() — question type, domain, difficulty classifiers
│   ├── export.py            # NEW: export_jsonl() — JSONL output, deterministic train/eval split
│   └── pipeline.py          # NEW: run_pipeline() — orchestrates ingest→chunk→enrich→export
├── embeddings/
│   ├── __init__.py          # (existing, empty)
│   ├── encoder.py           # NEW: EmbeddingEncoder — sentence-transformers wrapper, batch encoding, L2 norm
│   ├── indexer.py           # NEW: FAISSIndexer — IndexFlatIP, build/search/save/load
│   ├── metadata_store.py    # NEW: MetadataStore — JSON-backed dict, add/get/filter/save/load
│   └── pipeline.py          # NEW: run_embedding_pipeline() — load JSONL→embed→index→save
└── retrieval/
    ├── __init__.py          # (existing, empty)
    ├── models.py            # NEW: RetrievalRequest, RetrievalResult (Pydantic models)
    ├── ranking.py           # NEW: rerank() — weighted scoring (cosine_sim × alpha + metadata_boost × (1-alpha))
    └── retriever.py         # NEW: RetrieverService — compose embed+search+metadata+rank

tests/
├── conftest.py              # (existing) — extend with sample data fixtures
├── unit/
│   ├── test_ingestion.py    # NEW: Valid/malformed/edge-case ingestion tests
│   ├── test_chunking.py     # NEW: Chunk sizes, overlap, boundaries, unicode, determinism
│   ├── test_enrichment.py   # NEW: Known-answer classification tests
│   ├── test_encoder.py      # NEW: Single/batch encoding, cross-lingual similarity, normalization
│   ├── test_indexer.py      # NEW: Index build, search accuracy, save/load roundtrip
│   ├── test_metadata_store.py # NEW: CRUD, filtering, persistence
│   ├── test_retriever.py    # NEW: top-K, threshold, metadata filter, empty results
│   └── test_ranking.py      # NEW: Known scores → expected order, determinism
└── integration/
    ├── test_data_pipeline.py      # NEW: 100-row end-to-end data processing
    ├── test_embedding_pipeline.py # NEW: 100-chunk embed+index+search
    └── test_retrieval_pipeline.py # NEW: Natural language query → ranked results

data/
├── raw/                     # Downloaded dataset (gitignored)
├── processed/               # Pipeline output (gitignored)
│   ├── train.jsonl
│   ├── eval.jsonl
│   ├── index.faiss
│   └── metadata.json
└── evaluation/              # Reserved for Phase 3
```

**Structure Decision**: Single Python package (`src/mrag/`) with three submodules (`data/`, `embeddings/`, `retrieval/`) matching the constitution's modular architecture mandate. Each module is independently testable with its own contract interface. Existing foundation code (config, logging, exceptions) is reused without modification.

## Technical Approach

### Module 1: Data Processing (`src/mrag/data/`)

**Ingestion** (`ingestion.py`):
- Load CSV/JSON using `pandas.read_csv()` / `pandas.read_json()`
- Validate each row against `RawRecord` Pydantic model
- Skip and log invalid records; raise `DataProcessingError` only if 100% of records fail
- Normalize text: strip whitespace, normalize Unicode (NFC), handle encoding

**Chunking** (`chunking.py`):
- `TextChunker` class with configurable `chunk_size` (tokens) and `chunk_overlap` (tokens)
- Tokenization: split on whitespace for token counting (language-agnostic)
- Sentence splitting: regex-based on `.!?` + Unicode sentence terminators, with lookahead to handle abbreviations
- Sliding window: advance by `chunk_size - chunk_overlap` tokens per step, snap to nearest sentence boundary
- Edge cases: text shorter than chunk_size → single chunk; empty text → raise error

**Enrichment** (`enrichment.py`):
- Question type classifier: regex patterns on question prefix
  - `Who/What/When/Where/Which + ...?` → factoid
  - `How many/How much` → factoid
  - `List/Name (all|some|the)` → list
  - `Is/Are/Was/Were/Do/Does/Did/Can/Could + ...?` → yes_no
  - `Explain/Describe/Why/How (does|do|is|are)` → descriptive
  - Default → unknown
- Domain classifier: TF-IDF vectorizer fitted on predefined domain keywords, classify by highest-scoring domain
- Difficulty: `easy` if short answer present, `medium` if long-only, `hard` if answer is ambiguous or very long

**Export** (`export.py`):
- Serialize `ProcessedDocument` to JSON, write one per line to `.jsonl`
- Deterministic split: sort by `doc_id`, use `sklearn.model_selection.train_test_split` with fixed `random_state=42`
- Log statistics: total records, train count, eval count, total chunks

### Module 2: Embeddings & Indexing (`src/mrag/embeddings/`)

**Encoder** (`encoder.py`):
- Wrap `sentence_transformers.SentenceTransformer` with configured model name
- `encode()`: batch-process texts, L2-normalize output via `sklearn.preprocessing.normalize`
- `encode_single()`: convenience for single query embedding
- Lazy model loading: initialize model on first `encode()` call
- Log: model loaded, batch progress, total encoding time

**Indexer** (`indexer.py`):
- `FAISSIndexer` wraps `faiss.IndexFlatIP` (inner product = cosine on L2-normed vectors)
- `build_index()`: create fresh index, add all vectors
- `search()`: query with `index.search()`, return scores and integer IDs
- `save()`/`load()`: use `faiss.write_index()` / `faiss.read_index()`
- Validate dimension on build and search

**Metadata Store** (`metadata_store.py`):
- Python `dict[int, MetadataEntry]` in memory
- `add()`: insert by FAISS integer ID
- `get()`: O(1) lookup by ID
- `filter()`: linear scan with field-value match (acceptable for <1M entries)
- `save()`/`load()`: `json.dump()` / `json.load()` with custom encoder for Pydantic models

**Pipeline** (`pipeline.py`):
- Load train JSONL from data pipeline
- Extract all chunk texts + build metadata entries
- Batch-encode via `EmbeddingEncoder`
- Build FAISS index via `FAISSIndexer`
- Populate `MetadataStore`
- Save all artifacts to disk
- Log: chunks processed, index size, encoding time, total time

### Module 3: Retrieval (`src/mrag/retrieval/`)

**Models** (`models.py`):
- `RetrievalRequest`: Pydantic model with `query`, `top_k`, `score_threshold`, `metadata_filters`
- `RetrievalResult`: Pydantic model with all output fields (chunk_text, scores, metadata)

**Ranking** (`ranking.py`):
- `rerank()` function with configurable `alpha` (default 0.7)
- `relevance_score = alpha * cosine_similarity + (1 - alpha) * metadata_boost`
- Metadata boost computation:
  - +0.1 if `has_short_answer` (answer availability signal)
  - +0.1 if question_type matches query pattern (e.g., factoid query → factoid result)
  - Boost capped at 1.0
- Sort by relevance_score descending

**Retriever** (`retriever.py`):
- `RetrieverService` takes encoder, indexer, metadata_store as constructor dependencies
- `retrieve()` method orchestrates the full flow
- Over-fetch: if `score_threshold` is set, request `top_k * 2` from FAISS to account for post-filtering
- Metadata filter: post-filter results before re-ranking
- Return sorted, filtered results capped at `top_k`

## Key Technical Decisions

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Embedding model | `paraphrase-multilingual-MiniLM-L12-v2` (384 dims) | 50+ languages, CPU-feasible, per Article III |
| FAISS index | `IndexFlatIP` (exact search) | ~300K vectors, accuracy > speed per Article IV |
| Vector normalization | L2-normalize before indexing | Inner product on unit vectors = cosine similarity |
| Chunking | Sliding window, sentence-boundary aligned | Semantic coherence per Article II |
| Metadata storage | JSON dict keyed by int | O(1) lookup, no DB dependency, human-readable |
| Re-ranking | Weighted linear combination (alpha=0.7) | Beyond raw cosine per Article IV, interpretable |
| Export format | JSONL with fixed-seed 90/10 split | Streaming, deterministic per Article II |
| Data models | Pydantic v2 models for validation | Type safety at boundaries per Article IX |
| Question classification | Regex-based heuristics | Deterministic, no ML dependency, offline-capable |
| Batch size | 64 for embeddings | Memory/throughput balance on 16GB RAM |

## Complexity Tracking

No constitution violations. All decisions follow the simplest approach that satisfies requirements.

## Post-Design Constitution Re-Check

| Article | Pre-Design | Post-Design | Delta |
|---------|-----------|-------------|-------|
| I — Modular | PASS | PASS | Three modules with explicit contracts in `contracts/` |
| II — Data Integrity | PASS | PASS | Pydantic validation at every boundary, deterministic chunking |
| III — Multilingual | PASS | PASS | MiniLM multilingual model, Unicode-safe chunking |
| IV — Retrieval Quality | PASS | PASS | Exact search + weighted re-ranking with metadata signals |
| VI — Testing | PASS | PASS | 8 unit test files, 3 integration test files planned |
| VIII — Performance | PARTIAL | PARTIAL | Batch embedding, progress logging; caching is Phase 2 |
| IX — Code Quality | PASS | PASS | Type hints, structlog, black/ruff, pinned deps |
| X — Doc Separation | PASS | PASS | Spec has zero tech details; plan has all tech decisions |
